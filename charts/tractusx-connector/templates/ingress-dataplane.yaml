#################################################################################
#  Copyright (c) 2023 ZF Friedrichshafen AG
#  Copyright (c) 2023 Mercedes-Benz Tech Innovation GmbH
#  Copyright (c) 2023 Bayerische Motoren Werke Aktiengesellschaft (BMW AG)
#  Copyright (c) 2021,2023 Contributors to the Eclipse Foundation
#
#  See the NOTICE file(s) distributed with this work for additional
#  information regarding copyright ownership.
#
#  This program and the accompanying materials are made available under the
#  terms of the Apache License, Version 2.0 which is available at
#  https://www.apache.org/licenses/LICENSE-2.0.
#
#  Unless required by applicable law or agreed to in writing, software
#  distributed under the License is distributed on an "AS IS" BASIS, WITHOUT
#  WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied. See the
#  License for the specific language governing permissions and limitations
#  under the License.
#
#  SPDX-License-Identifier: Apache-2.0
#################################################################################


{{- $fullName := include "txdc.fullname" . }}
{{- $dataLabels := include "txdc.dataplane.labels" . }}
{{- $dataEdcEndpoints := .Values.dataplane.endpoints }}
{{- $namespace := .Release.Namespace }}

{{- range $i, $ingress := .Values.dataplane.ingresses }}
{{- if and $ingress.enabled $ingress.endpoints }}
{{- /* Derive a unique hostname from the single dataplane.fqdn: first ingress = fqdn, the rest are prefixed "internal-". A per-ingress hostname still overrides. */}}
{{- $hostname := $ingress.hostname }}
{{- if not $hostname }}
{{- $hostname = $.Values.dataplane.fqdn | required "Set dataplane.fqdn (or a per-ingress hostname)" }}
{{- if eq $i 1 }}{{- $hostname = printf "internal-%s" $hostname }}{{- else if gt $i 1 }}{{- $hostname = printf "internal%d-%s" $i $hostname }}{{- end }}
{{- end }}
{{- $dataIngressName := printf "%s-dataplane-%s" $fullName $hostname }}
{{- $annotations := $ingress.annotations | default dict }}
{{- if not (hasKey $annotations "external-dns.alpha.kubernetes.io/hostname") }}
{{- $_ := set $annotations "external-dns.alpha.kubernetes.io/hostname" $hostname }}
{{- end }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $dataIngressName }}
  namespace: {{ $namespace | default "default" | quote }}
  labels:
    {{- $dataLabels | nindent 4 }}
  annotations:
    {{- if $ingress.certManager }}
    {{- if $ingress.certManager.issuer }}
    {{- $_ := set $annotations "cert-manager.io/issuer" $ingress.certManager.issuer}}
    {{- end }}
    {{- if $ingress.certManager.clusterIssuer }}
    {{- $_ := set $annotations "cert-manager.io/cluster-issuer" $ingress.certManager.clusterIssuer}}
    {{- end }}
    {{- end }}
    {{- with $annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  {{- if $ingress.className }}
  ingressClassName: {{ $ingress.className }}
  {{- end }}
  {{- if $hostname }}
  {{- if $ingress.tls.enabled }}
  tls:
    - hosts:
        - {{ $hostname }}
      {{- if $ingress.tls.secretName }}
      secretName: {{ $ingress.tls.secretName }}
      {{- else }}
      secretName: {{ $dataIngressName }}-tls
      {{- end }}
  {{- end }}
  rules:
    - host: {{ $hostname }}
      http:
        paths:
          {{- $ingressEdcEndpoints := $ingress.endpoints }}
          {{- range  $name, $mapping := $dataEdcEndpoints }}
          {{- if (has $name $ingressEdcEndpoints) }}
          - path: {{ $mapping.path }}
            pathType: Prefix
            backend:
              service:
                name: {{ $fullName }}-dataplane
                port:
                  number: {{ $mapping.port }}
        {{- end }}
  {{- end }}
  {{- end }}
{{- end }}{{- /* end: if .enabled */}}
{{- end }}{{- /* end: range .Values.ingresses */}}
