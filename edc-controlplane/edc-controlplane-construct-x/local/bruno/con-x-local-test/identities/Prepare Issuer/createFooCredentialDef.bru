meta {
  name: createFooCredentialDef
  type: http
  seq: 8
}

post {
  url: {{ISSUER_ISS_API}}/v1alpha/participants/{{ISS_PART_CONT}}/credentialdefinitions
  body: json
  auth: inherit
}

body:json {
  {
    "attestations": [
      "dev-def-2"
    ],
    "credentialType": "FooCredential",
    "id": "dev-credential-def-2",
    "jsonSchema": "{}",
    "jsonSchemaUrl": "https://example.com/schema/dev-credential.json",
    "mappings": [
      {
        "input": "content",
        "output": "credentialSubject",
        "required": true
      }
    ],
    "rules": [],
    "format": "VC1_0_JWT",
    "validity": 15552000
  }
}

script:pre-request {
  req.setHeader("x-api-key", bru.getEnvVar("ISSUER_APIKEY"));
}

settings {
  encodeUrl: true
  timeout: 0
}
