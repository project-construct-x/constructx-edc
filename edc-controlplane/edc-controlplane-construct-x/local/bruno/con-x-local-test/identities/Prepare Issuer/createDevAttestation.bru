meta {
  name: createDevAttestation
  type: http
  seq: 5
}

post {
  url: {{ISSUER_ISS_API}}/v1beta/participants/{{ISS_PART_CONT}}/attestations
  body: json
  auth: none
}

headers {
  ~x-api-key: {{ISSUER_APIKEY}}
}

body:json {
  {
    "attestationType": "dev",
    "id": "dev-def-1",
    "configuration": {
      "default": {
        "isMember": true
      },
      "blackList": []
    }
  }
}

script:pre-request {
  req.setHeader("x-api-key", bru.getVar("ISSUER_APIKEY"));
}

settings {
  encodeUrl: true
  timeout: 0
}
